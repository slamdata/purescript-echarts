{
  "name": "purescript-echarts",
  "private": true,
  "devDependencies": {
    "echarts": "cryogenian/echarts#item-value-unshift-prebuild",
    "express": "^4.16.1"
  },
  "scripts": {
    "build": "spago build",
    "test": "spago test",
    "bb": "purs-backend-es build",
    "bundle": "purs-backend-es bundle-app --main Main --to dist/index.js"
  },
  "dependencies": {
    "esbuild": "^0.14.54",
    "purs-backend-es": "^1.1.0"
  }
}
