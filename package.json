{
  "name": "purescript-echarts",
  "private": true,
  "devDependencies": {
    "echarts": "cryogenian/echarts#item-value-unshift-prebuild",
    "express": "^4.16.1"
  },
  "scripts": {
    "build": "spago build",
    "test": "spago test",
    "esbuild --bundle index.js --servedir=dist"
  },
  "dependencies": {
    "esbuild": "^0.14.54",
    "purs-backend-es": "^1.1.0"
  }
}
